// Code generated by goa v3.23.3, DO NOT EDIT.
//
// persona HTTP client CLI support package
//
// Command:
// $ goa gen github.com/linuxfoundation/lfx-v2-persona-service/cmd/server/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	personaservicec "github.com/linuxfoundation/lfx-v2-persona-service/gen/http/persona_service/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"persona-service (livez|readyz)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "persona-service livez" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		personaServiceFlags = flag.NewFlagSet("persona-service", flag.ContinueOnError)

		personaServiceLivezFlags = flag.NewFlagSet("livez", flag.ExitOnError)

		personaServiceReadyzFlags = flag.NewFlagSet("readyz", flag.ExitOnError)
	)
	personaServiceFlags.Usage = personaServiceUsage
	personaServiceLivezFlags.Usage = personaServiceLivezUsage
	personaServiceReadyzFlags.Usage = personaServiceReadyzUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "persona-service":
			svcf = personaServiceFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "persona-service":
			switch epn {
			case "livez":
				epf = personaServiceLivezFlags

			case "readyz":
				epf = personaServiceReadyzFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "persona-service":
			c := personaservicec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "livez":
				endpoint = c.Livez()
			case "readyz":
				endpoint = c.Readyz()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// personaServiceUsage displays the usage of the persona-service command and
// its subcommands.
func personaServiceUsage() {
	fmt.Fprintln(os.Stderr, `Persona service health endpoints`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] persona-service COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    livez: Check if the service is alive.`)
	fmt.Fprintln(os.Stderr, `    readyz: Check if the service is ready to accept requests.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s persona-service COMMAND --help\n", os.Args[0])
}
func personaServiceLivezUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] persona-service livez", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is alive.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "persona-service livez")
}

func personaServiceReadyzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] persona-service readyz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is ready to accept requests.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "persona-service readyz")
}
